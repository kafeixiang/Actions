name: Build Private Code in Public Repo

on:
  workflow_dispatch:
    inputs:
      repo_and_branch:
        description: '🚀 选择【私有项目】与【目标分支】'
        required: true
        default: 'kafeixiang/husi [dev-3]'
        type: choice
        options:
          - 'kafeixiang/husi [dev-3]'
          - 'kafeixiang/husi [main]'
          - 'kafeixiang/master [master]'
          - 'kafeixiang/Exclave [dev]'
          - 'kafeixiang/Exclave [main]'
          - 'kafeixiang/ClashMetaForAndroid [main]'
          - 'kafeixiang/ClashMetaForAndroid [dev]'
          - 'kafeixiang/v2rayNG [master]'
          - 'kafeixiang/v2rayNG [dev]'
          - 'kafeixiang/NekoBoxForAndroid [main]'
          - 'kafeixiang/NekoBoxForAndroid [dev]'
          - 'kafeixiang/FlClash [main]'
          - 'kafeixiang/FlClash [dev]'
          - 'kafeixiang/v2rayN [master]'
          - 'kafeixiang/v2rayN [wpf]'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  setup_matrix:
    runs-on: ubuntu-latest
    outputs:
      os_runner: ${{ steps.set_os.outputs.os_runner }}
    steps:
      - id: set_os
        run: |
          RAW="${{ github.event.inputs.repo_and_branch }}"
          if echo "$RAW" | grep -q "v2rayN"; then
            echo "os_runner=windows-latest" >> $GITHUB_OUTPUT
          else
            echo "os_runner=ubuntu-24.04" >> $GITHUB_OUTPUT
          fi

  build_using_native_script:
    needs: setup_matrix
    runs-on: ${{ needs.setup_matrix.outputs.os_runner }}

    steps:
    # 1. 解析菜单选择
    - name: Parse Selection
      id: parse
      shell: bash
      run: |
        RAW="${{ github.event.inputs.repo_and_branch }}"
        REPO=$(echo "$RAW" | awk '{print $1}')
        BRANCH=$(echo "$RAW" | grep -oP '\[\K[^\]]+')
        PROJECT_NAME=$(echo "$REPO" | cut -d'/' -f2)
        
        echo "TARGET_REPO=$REPO" >> $GITHUB_ENV
        echo "TARGET_BRANCH=$BRANCH" >> $GITHUB_ENV
        echo "PROJECT_NAME=$PROJECT_NAME" >> $GITHUB_ENV

    # 2. 检出目标私有仓库
    - name: Checkout Private Source
      uses: actions/checkout@v4
      with:
        repository: ${{ env.TARGET_REPO }}
        token: ${{ secrets.PAT_TOKEN }}
        ref: ${{ env.TARGET_BRANCH }}
        fetch-depth: 1

    # 3. 深度递归拉取所有端底层子模块
    - name: Clone Submodules
      shell: bash
      run: |
        git config --global url."https://${{ secrets.PAT_TOKEN }}@github.com/".insteadOf "https://github.com/"
        git submodule update --init --recursive --depth=1

    # 4. 配置 Java 环境
    - name: Set up Java Runtime
      if: env.PROJECT_NAME != 'v2rayN'
      uses: actions/setup-java@v4
      with:
        java-version: 21
        distribution: 'temurin'

    # 5. 配置 Go 环境
    - name: Set up Go Runtime
      if: env.PROJECT_NAME == 'Exclave' || env.PROJECT_NAME == 'NekoBoxForAndroid' || env.PROJECT_NAME == 'husi' || env.PROJECT_NAME == 'ClashMetaForAndroid'
      uses: actions/setup-go@v5
      with:
        go-version: 1.26.x
        cache: true 
        cache-dependency-path: "**/go.sum"

    # 6. 挂载 Gradle 编译加速缓存
    - name: Setup Gradle Cache for Fast Build
      if: env.PROJECT_NAME != 'v2rayN' && env.PROJECT_NAME != 'FlClash'
      uses: gradle/actions/setup-gradle@v3
      with:
        cache-read-only: false 

    # 7. 配置 .NET / Flutter 环境
    - name: Set up .NET SDK
      if: env.PROJECT_NAME == 'v2rayN'
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Set up Flutter
      if: env.PROJECT_NAME == 'FlClash'
      uses: subosito/flutter-action@v2
      with:
        channel: 'stable'
        cache: true

    # 8. 【husi dev-3 专项补丁】部署 Linux 编译 Apple Darwin 的必备底层依赖
    - name: Inject Rigorous Linux-to-Mac Cross-Compilation Toolchain
      if: env.PROJECT_NAME == 'husi'
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install -y clang llvm libxml2-dev uuid-dev patch libssl-dev make
        echo "CC=clang" >> $GITHUB_ENV
        echo "CXX=clang++" >> $GITHUB_ENV

    # 9. 挂载本地 Android SDK/NDK 映射属性
    - name: Prepare Local Properties
      if: env.PROJECT_NAME != 'v2rayN'
      shell: bash
      run: |
        echo "sdk.dir=$ANDROID_HOME" > local.properties
        if [ "${{ env.PROJECT_NAME }}" = "ClashMetaForAndroid" ]; then
          NDK_PATH=$(ls -d $ANDROID_HOME/ndk/25* 2>/dev/null || ls -d $ANDROID_HOME/ndk/26* 2>/dev/null || ls -d $ANDROID_HOME/ndk/* | tail -n 1)
        else
          NDK_PATH=$(ls -d $ANDROID_HOME/ndk/* 2>/dev/null | sort -V | tail -n 1 || echo "")
        fi
        if [ ! -z "$NDK_PATH" ]; then
          echo "ndk.dir=$NDK_PATH" >> local.properties
          echo "ANDROID_NDK_HOME=$NDK_PATH" >> $GITHUB_ENV
        fi

    # =================================================================
    # 核心步骤隔离：现场本地编译专属魔改版 GoMobile 工具链
    # =================================================================
    - name: Compile and Install Matsuri Custom GoMobile
      if: env.PROJECT_NAME == 'NekoBoxForAndroid'
      shell: bash
      run: |
        echo "⚙️ 正在就地独立编译 Matsuri 魔改版工具链..."
        mkdir -p "${{ github.workspace }}/gm-build-bin"
        cd "${{ github.workspace }}/gm-build-bin"
        git init
        git remote add origin https://github.com/MatsuriDayo/gomobile.git
        git fetch --depth=1 origin
        git checkout FETCH_HEAD
        go install ./cmd/gomobile
        go install ./cmd/gobind
        echo "$HOME/go/bin" >> $GITHUB_PATH

    # =================================================================
    # 核心步骤隔离：强行物理平铺源码依赖，阻断相对路径无法识别的大坑
    # =================================================================
    - name: Hardcore Force Bind Kernel AAR
      if: env.PROJECT_NAME == 'NekoBoxForAndroid'
      shell: bash
      run: |
        export CGO_ENABLED=1
        cd "$GITHUB_WORKSPACE"
        
        # 自动捞取主应用下真正的 Go 核心层模块目录位置
        GO_CORE_PATH=$(find . -name "go.mod" -not -path "*/build/*" -not -path "*/gm-build-bin/*" | head -n 1)
        if [ ! -z "$GO_CORE_PATH" ]; then
          GO_CORE_DIR=$(dirname "$GO_CORE_PATH")
          echo "🎯 进驻底层 Go 源码工程：$GO_CORE_DIR"
          
          # 💡 【物理覆写：彻底斩断断链盲区】
          # 用 find 去精准捕捉递归拉下来的 sing-box 和 libneko
          FIND_SUB_SB=$(find "$GITHUB_WORKSPACE" -maxdepth 4 -name "go.mod" | grep "sing-box" | head -n 1 || echo "")
          FIND_SUB_LN=$(find "$GITHUB_WORKSPACE" -maxdepth 4 -name "go.mod" | grep "libneko" | head -n 1 || echo "")
          
          # 建立真实的、硬核物理隔离的上游父级文件夹
          mkdir -p "$GITHUB_WORKSPACE/../sing-box" "$GITHUB_WORKSPACE/../libneko"
          
          # 直接进行全量源码级别的强制同步覆盖
          if [ ! -z "$FIND_SUB_SB" ]; then cp -r "$(dirname "$FIND_SUB_SB")"/. "$GITHUB_WORKSPACE/../sing-box/"; fi
          if [ ! -z "$FIND_SUB_LN" ]; then cp -r "$(dirname "$FIND_SUB_LN")"/. "$GITHUB_WORKSPACE/../libneko/"; fi
          
          cd "$GO_CORE_DIR"
          
          # 自动补全可能缺失的动态声明骨架
          if ! grep -q "module " go.mod; then
            sed -i '1s/^/module libcore\n\n/' go.mod
          fi
          
          go mod tidy || true
          $HOME/go/bin/gomobile init || gomobile init
          $HOME/go/bin/gomobile bind -v -androidapi 21 -target android -o libcore.aar . || true
          
          mkdir -p "$GITHUB_WORKSPACE/app/libs"
          cp -f libcore.aar "$GITHUB_WORKSPACE/app/libs/" 2>/dev/null || true
          cp -f *.jar "$GITHUB_WORKSPACE/app/libs/" 2>/dev/null || true
          cd "$GITHUB_WORKSPACE"
        fi

    # =================================================================
    # 执行层：纯净多项目原厂编译总控
    # =================================================================
    - name: Reference and Execute Native Project Build Process
      shell: bash
      run: |
        echo "==== 正在启动原厂 CI 闭环打包线程 ===="
        export CGO_ENABLED=1
        
        case "${{ env.PROJECT_NAME }}" in
          
          "husi")
            find . -name "*.sh" -o -name "gradlew" -o -name "Makefile" | xargs chmod +x || true
            if [ -f "build.sh" ]; then
              ./build.sh --all || ./build.sh macos || ./build.sh
            elif [ -f "Makefile" ] || [ -f "makefile" ]; then
              make macos || make all || make
            else
              export GOOS=darwin GOARCH=amd64
              go build -v -ldflags="-s -w" -o husi-mac-amd64 .
              export GOOS=darwin GOARCH=arm64
              go build -v -ldflags="-s -w" -o husi-mac-arm64 .
            fi
            if [ -f "gradlew" ]; then
              ./gradlew assembleRelease --stacktrace
            fi
            ;;

          "NekoBoxForAndroid")
            cd "$GITHUB_WORKSPACE"
            echo "go.dir=$(which go | sed 's/\/bin\/go//')" >> local.properties
            chmod +x gradlew
            echo "🚀 物理依赖槽已完全灌满，拉起原厂变体打包主进程..."
            ./gradlew :app:assembleFdroidRelease --stacktrace
            ;;
            
          "ClashMetaForAndroid")
            chmod +x gradlew
            ./gradlew :core:assembleRelease || true
            ./gradlew assembleRelease --stacktrace
            ;;
            
          "Exclave"|"v2rayNG")
            chmod +x gradlew
            ./gradlew assembleRelease
            ;;
            
          "FlClash")
            if [ -f "android/app/build.gradle" ]; then
              sed -i 's/signingConfigs.release/signingConfigs.debug/g' android/app/build.gradle
            fi
            flutter pub get
            flutter build apk --release
            if flutter devices | grep -q "linux"; then
               flutter build linux --release || true
            fi
            ;;
            
          *)
            echo "跳过 Linux 执行"
            ;;
        esac

    # 🟢 针对 v2rayN Windows 桌面端
    - name: Execute Native Project Build Process [Windows v2rayN]
      if: env.PROJECT_NAME == 'v2rayN'
      shell: pwsh
      run: |
        dotnet restore
        dotnet publish v2rayN/v2rayN.csproj -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true /p:PublishReadyToRun=true

    # =================================================================
    # 产物层
    # =================================================================
    - name: Archive and Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ${{ env.PROJECT_NAME }}-${{ env.TARGET_BRANCH }}-Artifacts
        path: |
          app/build/outputs/apk/**/*.apk
          app/build/outputs/bundle/**/*.aab
          build/app/outputs/flutter-apk/*.apk
          build/linux/x64/release/bundle/**/*
          v2rayN/bin/Release/net8.0/win-x64/publish/*
          bin/Release/net8.0/win-x64/publish/*
          **/husi-mac*
          **/*.dmg
          **/*.app
          **/*.zip
          **/*.tar.gz
          *.apk
          *.aar
        if-no-files-found: warn
